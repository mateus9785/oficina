# ---------------------------------------------------------------------------
# Backend API base URL. Defaults to http://localhost:3001/api/v1 if unset
# (see src/lib/api.ts), so this file only matters when the backend runs on a
# different host/port.
# ---------------------------------------------------------------------------
VITE_API_URL=http://localhost:3001/api/v1
